name: printer_ip_command
description: Network printer probing and ESC/POS driver for IP-based thermal printers.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.6.0

dependencies:
  enough_convert: ^1.6.0
  gbk_codec: ^0.4.0
  multicast_dns: ^0.3.2

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
